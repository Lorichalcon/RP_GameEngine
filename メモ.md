# RP Game Engine 開発進捗メモ (2026-03-17)

## 🛠 本日実装した機能

### 1. パーティシステム (3スロット対応)
- 最大3人までのキャラクターを同時に読み込み、管理できるようにしました。
- 各スロットに固有の「名前」「性格」「設定」「アイコン画像」を保持できます。
- エディター内にスロット切り替えタブを実装し、キャラごとに個別編集が可能。
- スロット内のデータを初期化する「Clear This Slot」機能を追加。

### 2. インテリジェント・パーティプロンプト
- 複数キャラがいる場合、AIに対して「全員の役を演じること」「発言の頭に名前を付けること」を自動で指示。
- 全アクティブキャラの設定（Description, Scenario等）を結合してAIに送信する「合体プロンプト」を実装。

### 3. パーティ・チャットUIの高度化
- **アバター自動切り替え**: AIの返答から「誰が喋っているか」を自動解析し、そのキャラのアイコンを吹き出しに表示。
- **マルチバブル表示**: AIが複数キャラのセリフを一度に生成した場合、キャラごとに別の吹き出しとして分割して表示。
- **掛け合いボタン (Banter)**: プレイヤーが沈黙し、NPC同士で会話をさせるための専用ボタン（オレンジ色の💬）を実装。

### 4. 設定管理とUX改善
- **Lorebookの統合**: 各キャラのスロット内に世界観設定（Lorebook）を組み込み、キャラ個別の知識を持たせられるように。
- **パーティシート表示**: Character Sheetタブで、パーティ全員のステータスカードを縦に並べて確認できるよう改修。
- **自動保存の強化**: スロット、チャット履歴、ユーザー設定をブラウザに永続保存（パーティ構成が変わっても履歴が混ざらないようID管理を導入）。

---

## 🚀 次に実装すべき・検討したい機能案

1. **共有ワールドブック (Common Lore)**
   - 全キャラで共通して使う世界観設定（場所や時代背景など）を、キャラ個別とは別に管理する枠組み。
2. **チャット履歴の編集・削除**
   - AIの返答が気に入らない場合に、一部分だけ書き換えたり削除したりして微調整する機能。
3. **パーティ一括エクスポート/インポート**
   - 現在は1人ずつJSONで出していますが、3人セット（パーティ全体）を1ファイルで保存・配布できる機能。
4. **掛け合いの制御設定**
   - 掛け合いボタンを押した際、「誰から話し始めるか」「何往復するか」などを簡易指定できるオプション。
5. **画像生成連携の実験**
   - キャラクターを新規作成する際、アイコン画像をAIで生成してそのままセットできる機能。

---
お疲れ様でした！ゆっくりお休みください。